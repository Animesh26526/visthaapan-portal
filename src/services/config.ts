// Config determines whether to use mock APIs or real APIs
export const USE_MOCK_API = import.meta.env.VITE_USE_MOCK_API !== 'false';

// Simulated network delay for mock services
export const MOCK_DELAY_MS = 500;
